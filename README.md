# Circular-Compartment-Train
A novel sub-linear data structure combining a cyclic doubly-linked node ring ("the train") with a dynamic centralized radial partition index ("compartments"). Governed by an adaptive equilibrium bound, it scales unique sets to O(sqrt(N)) search/insert boundaries while executing structural duplicate filtering in constant O(1) time.
